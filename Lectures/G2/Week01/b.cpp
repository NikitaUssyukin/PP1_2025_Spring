#include <iostream> 

using namespace std; 

int main() {

    /*
    data types:
    
    1. int/long/long long - for integer numbers, e.g. 2, -2, 0, 5, 1234, etc
    2. double/float - for decimal fractions, e.g. 3.14, 2.5, 10.1, -2.3, 0.23, etc
    3. char - for storing singular characters, e.g. 'A', 'a', '1', '0', '#', '%', ' ', '.', etc
        - Important: Singular quotes signify that the symbol inside is of char data type 
    4. bool - stores boolean values, meaning either 1 or 0, either true or false
    5. string - for storing sequences of characters, e.g. "Hello", "a", "abc", "12345", "the weather is cold"
        - Important: Double quotes signify that the sequence of symbols inside is of string data type 
    */ 

    // declaring variables, examples:
    int a;
    double b;
    char c;
    bool d;
    string e;

    return 0;
}