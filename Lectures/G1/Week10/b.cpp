#include <iostream>

using namespace std;

int main() {

    /*
    Taking input into a string
    - using cin we can input only the strings that do not contain spaces
    (or any whitespace characters, for that matter)
    */

    string s;

    cin >> s;

    cout << s << endl;

    return 0;
}